package day1;

public class parametarization {
	
	/*---------------------------Parametarization------------------
	 * so far we have used different levels of variables now lets see how parameters are used in postman
	 * same as like dat of variables we have to set the parameter values and if we want to send mutiple parameters that are present in
	 * CSV format or json format we have to import the file from location path and have to select the iteration time and then execute 
	 * depend on the number of sets data that present in file those num of requests will execute.
	 */

}
